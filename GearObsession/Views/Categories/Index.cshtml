@{
  Layout = "_Layout";
}

@using GearObsession.Models;

<h1>Categories</h1>

@if (@Model.Count == 0)
{
  <h3>No categories have been added yet!</h3>
} 

<ul>
  @foreach(Category category in Model)
  {
    <li>@Html.ActionLink($"{category.Name}", "Details", new { id = category.CategoryId })</li>
  }
</ul>

</table> 
<a href="/Categories/Create"><button type="button" class="btn btn-primary">Add New Category</button></a>
<a href="/Items/Create"><button type="button" class="btn btn-success">Add New Item</button></a>